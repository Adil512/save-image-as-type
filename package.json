{
  "name": "save-image-as-type",
  "version": "1.0.0",
  "description": "Chrome extension to save images in different formats",
  "scripts": {
    "test": "jest",
    "test:unit": "jest unit",
    "test:integration": "jest integration",
    "test:e2e": "jest e2e"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-chrome": "^0.8.0",
    "puppeteer": "^19.0.0"
  }
}
{
  "scripts": {
    "bump:major": "node scripts/bump-version.js major",
    "bump:minor": "node scripts/bump-version.js minor",
    "bump:patch": "node scripts/bump-version.js patch",
    "release": "npm run test && npm run build && git push --follow-tags"
  }
}
